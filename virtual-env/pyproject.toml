[project]
name = "virtual-env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sanjanakanavi", email = "sanjanakanavi2000@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.1",
]

[project.scripts]
virtual-env = "virtual_env:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
