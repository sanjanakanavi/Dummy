#decorators, generators, iterators, context managers, lambda functions, map(), filter(), reduce(), type hints, data classes
from typing import List, Optional, Any

brand = Optional[str] = "bmw"
print(brand)